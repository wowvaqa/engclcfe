/**
 *
 * @param {width, height -> width & height of final image} props
 */
const DynamicDraw = (props) => {
  // const [width, setWidth] = useState(props.width);
  // const [height, setHeight] = useState(props.height);
};

export default DynamicDraw;
